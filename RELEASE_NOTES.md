## 本次更新亮点

<!--
这份文档是 **下一次 GitHub Release 的封面段**，会被 .github/workflows/release.yaml
自动读取并拼到 release body 顶部（位于版本号之下、git log changelog 之上）。

写作规则（详见 AGENTS.md「Release Flow」一节）：
- 每次大型改动合入 main 时，必须在「本次更新亮点」中追加要点；
- 措辞面向用户（讲清"用户能感觉到什么变化"），不要写实现细节或 commit hash；
- 发版（打 tag）后请人工把当前内容归档到下方「历史发布」段落，并清空「本次更新亮点」准备下一轮；
- 文件为空 / 不存在时 release workflow 会跳过该段，不影响发版。

什么算"大型改动"：
- 新增页面 / 新增设置项 / 新增数据源
- Bangumi、播放器、下载、同步等核心模块的行为变化
- Hive 模型变更或数据迁移
- 修复影响大量用户的 bug
- 依赖大版本升级、平台支持变化
小幅 UI 微调、内部重构、注释/文档变更可以不写。
-->

- 新增「看完弹评分短评」：整部番剧最后一集播完后，可一键填写评分与短评直接同步到 Bangumi；信息页同步新增「写短评 / 我的评价」卡片，可随时回看与编辑
- Bangumi 配置页新增「看完弹评分短评」开关与「重置评价提示忽略列表」入口

## 已知问题

<!-- 可选；没有可整段删除 -->

## 历史发布

<!-- 发版后把上一轮「本次更新亮点」归档到这里，按版本倒序累积 -->
